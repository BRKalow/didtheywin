<!DOCTYPE html>
<html ng-app="DidTheyWin">
<head>
  <title>Did They Win?</title>
  <script src="http://ajax.googleapis.com/ajax/libs/angularjs/1.2.18/angular.min.js"></script>
  <script src="http://ajax.googleapis.com/ajax/libs/angularjs/1.2.18/angular-animate.min.js"></script>
  <script src="/js/angucomplete.js"></script>
  <script src="/js/app.js"></script>
  <link href="http://maxcdn.bootstrapcdn.com/bootstrap/3.1.1/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="http://cdn.jsdelivr.net/animatecss/3.1.0/animate.min.css">
  <style type="text/css">
  @import url(http://fonts.googleapis.com/css?family=Lato);
  body {
    background: url(/images/body_bg.jpg);
    background-size: fill;
    background-position: 50% 0%;
    background-repeat: no-repeat;
    padding-top: 100px;
    font-family: 'Lato';
  }
  h1.title {
    color: #fff;
    margin-bottom: 50px;
    -webkit-animation-duration: 1s;
  }
  form.check-form {
    -webkit-animation-delay: 1.5s;
  }
  .result {
    background: white;
    margin-top: 20px;
    padding-top: 20px;
    padding-bottom: 20px;
    border-radius: 3px;
  }
  .result .team-top {
    border-bottom: 1px solid lightgrey;
    padding: 0px 0px 20px 0px;
  }
  .result .team-bottom {
    padding: 20px 0px 0px 0px;
  }
  .result .logo-wrapper {
    padding: 20px 0px;
    text-align: center;
    float: left;
    width: 160px;
  }
  .result img.logo {
    height: 60px;
  }
  .result h3 > small {
    color: #333;
  }
  .result.ng-hide-remove {
    -webkit-animation: flipInX 1s;
  }
  .result .points {
    font-size: 2em;
    padding: 30px 0px 20px 0px;
    float: right;
    display: inline-block;
    width: 150px;
    text-align: center;
  }
  .loser {
    opacity: 0.6;
  }
  .winner .points {
    color: green;
  }
  .date {
    position: absolute;
    left: 75%;
    top: 42%;
    background: white;
    border-radius: 100px;
    padding: 10px 15px;
    z-index:  100;
    font-size: 1em;
    color: darkgrey;
    -webkit-box-shadow: 0 2px 10px 0 rgba(0,0,0,.16);
    box-shadow: 0 2px 10px 0 rgba(0,0,0,.16)
  }
  .navbar-inverse {
    background: rgba(0,0,0,0.7);
    border-color: transparent;
  }
  .angucomplete-holder {
      position: relative;
      display: inline-block;
  }

  .angucomplete-dropdown {
      border-color: #ececec;
      border-width: 1px;
      border-style: solid;
      border-radius: 2px;
      width: 250px;
      padding: 6px;
      cursor: pointer;
      z-index: 9999;
      position: absolute;
      /*top: 32px;
      left: 0px;
      */
      margin-top: -6px;
      background-color: #ffffff;
  }

  .angucomplete-searching {
      color: #acacac;
      font-size: 14px;
  }

  .angucomplete-description {
      font-size: 14px;
  }

  .angucomplete-row {
      padding: 5px;
      color: #000000;
      margin-bottom: 4px;

  }

  .angucomplete-selected-row, .angucomplete-row:hover {
      background-color: lightblue;
      color: #ffffff;
  }

  .angucomplete-image-holder {
      padding-top: 2px;
      float: left;
      margin-right: 10px;
      margin-left: 5px;
  }

  .angucomplete-image {
      height: 34px;
      width: 34px;
      border-radius: 50%;
      border-color: #ececec;
      border-style: solid;
      border-width: 1px;
  }

  .angucomplete-image-default {
      /* Add your own default image here
       background-image: url('/assets/default.png');
      */
      background-position: center;
      background-size: contain;
      height: 34px;
      width: 34px;
  }
  </style>
</head>
<body ng-controller="MainCtrl">
  <div class="container">
    <div class="row">
      <div class="col-md-4">
        <div class="row">
          <div class="col-md-12">
            <h1 class="title" ng-class="{'animated bounceInLeft': !entrance}">
              Did They Win? <br/>
              <small>Missed the game? No problem! Check the result here.</small>
            </h1>
          </div>
        </div>
        <div class="row">
          <div class="col-md-12">
            <form name="check" class="form-inline check-form" ng-class="{'animated pulse': !entrance}">
              <angucomplete id="team"
                            placeholder="Find your team..."
                            pause="100"
                            selectedObject="team"
                            localdata="team_names"
                            searchfields="name"
                            titlefield="name"
                            inputclass="form-control input-lg">
              </angucomplete>
              <input type="button" value="Check" class="btn btn-primary btn-lg" ng-click="submit_form()" />
            </form>
          </div>
        </div>
      </div>
      <div class="col-md-2"></div>
      <div class="col-md-6 result" ng-show="result && result.hasOwnProperty('team_name' )">
        <span class="date">{{ result.date }}</span>
        <div class="row team-top" ng-class="{'winner': result.outcome == 'win', 'loser': result.outcome == 'loss'}">
          <div class="col-md-12">
            <div class="logo-wrapper">
              <img class="logo" ng-if="result && result.hasOwnProperty('team_id')" ng-src="/images/logos/{{ result.team_id }}.gif" alt="{{ result.team_name }}" />
            </div>
            <span class="points">{{ result.points_scored }}</span>
          </div>
        </div>
        <div class="row team-bottom" ng-class="{'loser': result.outcome == 'win', 'winner': result.outcome == 'loss'}">
          <div class="col-md-12">
            <div class="logo-wrapper">
              <img class="logo" ng-if="result && result.hasOwnProperty('team_id_opponent')" ng-src="/images/logos/{{ result.team_id_opponent }}.gif" alt="{{ result.team_name_opponent }}" />
            </div>
            <span class="points">{{ result.points_scored_opponent }}</span>
          </div>
        </div>
      </div>
    </div>
  </div>
  <nav class="navbar navbar-inverse navbar-fixed-bottom" role="navigation">
    <div class="container">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#collapse-bot-nav">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
      </div>
      <div class="collapse navbar-collapse" id="collapse-bot-nav">
        <ul class="nav navbar-nav navbar-right">
          <li><a href="http://brycekalow.name">Bryce Kalow</a></li>
          <li><a href="https://github.com/BRKalow/didtheywin">Github</a></li>
          <li><a href="https://erikberg.com/api">xmlstats API</a></li>
    </div>
  </nav>
</body>
</html>
